# 视频壁纸插件说明书 / Video Wallpaper Plugin Manual

## 版本 / Version: v1.6

---

## 中文说明

### 功能介绍

视频壁纸插件可以为 iNextOS / RoceOS 系统的登录页面设置动态视频背景。

**v1.6 新增功能：**
- ✅ **实时切换壁纸**：在管理页面点击视频后，登录页面壁纸立即自动切换，无需手动刷新页面
- ✅ **视频封面缩略图**：每个视频自动生成第一帧预览图，一目了然

### 安装方法

1. 将安装包上传到软路由
2. 解压：`tar -xzf video-wallpaper-v1.6.tar.gz`
3. 进入目录：`cd video-wallpaper`
4. 执行安装：`bash install.sh`
5. 安装完成后，桌面会自动创建快捷方式

### 访问地址

- 管理页面：http://192.168.100.1:8686
- 桌面快捷方式：自动创建

### 使用说明

1. **启用壁纸**：点击 "Enable" 按钮开启视频壁纸
2. **选择视频**：点击视频列表中的任意视频，壁纸会**实时切换**（无需刷新页面）
3. **调整透明度**：拖动 Opacity 滑块
4. **静音开关**：勾选/取消 Mute 复选框
5. **上传视频**：拖拽或点击上传区域，支持 MP4/WebM/OGG 直接播放，其他格式自动转码
6. **删除视频**：选中视频后点击 "Delete Selected" 按钮
7. **自定义视频目录**：在 Video Directory 中修改路径，支持浏览和新建文件夹

### 视频目录

- 默认目录：`/opt/video-wallpaper/videos`
- 可自定义为任意路径（如 `/mnt/sata6-1/wallpapers`）
- 更改路径不会删除已有视频

### 卸载方法

```bash
bash uninstall.sh
```

### 注意事项

- 首次安装需要构建 Docker 镜像（约 1-2 分钟）
- 视频文件最大支持 10GB
- 转码过程中会显示进度条，失败自动删除原文件并提示
- 系统更新后壁纸配置自动保留

---

## English Documentation

### Features

Video Wallpaper Plugin sets a dynamic video background for the iNextOS / RoceOS login page.

**New in v1.6:**
- ✅ **Live Wallpaper Switch**: Click a video in the manager, the wallpaper changes instantly — no page refresh needed
- ✅ **Video Thumbnails**: Auto-generates a first-frame preview for each video

### Installation

1. Upload the package to your router
2. Extract: `tar -xzf video-wallpaper-v1.6.tar.gz`
3. Enter directory: `cd video-wallpaper`
4. Install: `bash install.sh`
5. A desktop shortcut is created automatically

### Access

- Manager: http://192.168.100.1:8686
- Desktop shortcut: auto-created

### Usage

1. **Enable**: Click "Enable" to activate video wallpaper
2. **Select video**: Click any video in the list — wallpaper switches **in real time** (no refresh needed)
3. **Opacity**: Drag the Opacity slider
4. **Mute toggle**: Check/uncheck the Mute checkbox
5. **Upload**: Drag & drop or click the upload area. MP4/WebM/OGG play directly; other formats auto-transcode
6. **Delete**: Select a video and click "Delete Selected"
7. **Custom video directory**: Modify the path in Video Directory; browse and create folders supported

### Video Directory

- Default: `/opt/video-wallpaper/videos`
- Customizable to any path (e.g., `/mnt/sata6-1/wallpapers`)
- Changing path does not delete existing videos

### Uninstall

```bash
bash uninstall.sh
```

### Notes

- First install builds a Docker image (~1-2 minutes)
- Max file size: 10GB
- Transcoding shows a progress bar; failed files are auto-deleted with a notification
- Wallpaper config persists after system updates
